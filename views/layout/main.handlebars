<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../public/css/style.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Lora:ital@1&family=My+Soul&display=swap&family=Playfair+Display:ital@1&display=swap">

    <title>Tech Blog in The Rain</title>
</head>
<body>
    <div class="wrapper">
        <header>
            <nav>
                <a href="/" class="nav-link">Home</a>
                {{#if loggedIn}}
                <a href="/dashboard" class="nav-link">Dashboard</a>
                <button id="logout" class="logout">Logout</button>
                {{else}}
                <a href="/login">Login</a>
                {{/if}}
            </nav>
        </header>

        <main>
            {{{body}}}
        </main>

        <footer>
            <div>Thank You for Visiting!</div>
            <div>....and the moon looks very bright tonight huh? Why don't.....you look up ;)</div>
        </footer>
    </div>

    {{#if loggedIn}}
    <script src="../../public/js/logout.js"></script>
    {{/if}}
</body>
</html>